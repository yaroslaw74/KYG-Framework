# https://github.com/jawira/doctrine-diagram-bundle
doctrine_diagram:
    er:
        filename: '%kernel.project_dir%/test/er'
        size: midi
    class:
        filename: '%kernel.project_dir%/test/class'
        size: midi
    convert:
        format: svg
        converter: auto
